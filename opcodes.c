#include "monty.h"
int opcode_search(const char *opcode)
{

opcode_t codes[] = {
    {"push", push},
    {"pall", pall},

    {NULL, NULL}
};

int size = sizeof(codes) / sizeof(opcode_t) - 1;

while (i < size)
{
    if (codes[])
}
return (EXIT_SUCCESS);
}